dat <- data.frame(variable = c("12 20 58"))
library(tidyr)
dat <- separate(dat, variable, c("V1", "V2", "V3"), " ")

# * Creating vectors/matrices/data frames
# * Basic rules of probability
# * Combinations, permutations
# * Discrete random variables
# * E(X) and Var(X)

# effective class engagement
# active class engagement